# 指示内容
- ビルドしたら起動はCodex側で行う運用を AGENTS.md に記載する。

# 実施内容
- プロジェクトルートに `AGENTS.md` を新規作成。
- 以下の運用ルールを追記:
  - ビルド後は最終報告前に起動確認まで実施する。
  - 可能なら実際に起動して結果を報告する。
  - 起動確認不可の環境では理由を明記する。

# 課題、検討事項
- 既存のグローバル `~/.codex/AGENTS.md` に対するローカル上書きとして機能する想定。
- 起動確認の具体的コマンドは対象（CLI/.app）ごとに実施時に明記する。
