#!/bin/zsh
set -euo pipefail

# 使い方:
#   tproj        # 現在ディレクトリからプロジェクト起動（更新あり）
#   tproj -n     # アップデートなしで起動（オフライン用）

NO_UPDATE=false
[[ "${1:-}" == "-n" || "${1:-}" == "--no-update" ]] && NO_UPDATE=true

root="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"

# セッション存在チェック関数
has_claude_session() {
  local abs_path="$1"
  local encoded="${abs_path//\//-}"
  [[ -f "$HOME/.claude/projects/$encoded/sessions-index.json" ]]
}

has_codex_session() {
  local abs_path="$1"
  find "$HOME/.codex/sessions" -type f -name "*.jsonl" -print0 2>/dev/null | \
    xargs -0 grep -l "\"cwd\":\"$abs_path\"" 2>/dev/null | head -1 | grep -q .
}
name="$(basename "$root")"

# tmuxセッション名に使えない文字を潰す
session="${name//[^A-Za-z0-9_-]/_}"

# “プロジェクトのルート”を作業ディレクトリにする（gitならroot、無ければcwd）
root="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"

# 既存セッションがあればアタッチ、無ければ作る
if ! tmux has-session -t "$session" 2>/dev/null; then
  # 1) dev window を作成
  tmux new-session -d -s "$session" -c "$root" -n dev

  # 2) VSCodeっぽい3ペイン（左メイン / 右上watch / 右下logs）
  tmux split-window -h -t "$session:dev" -c "$root"
  tmux split-window -v -t "$session:dev.2" -c "$root" -l 70%

  # 3) セッション有無に応じた起動コマンドを構築
  if has_claude_session "$root"; then
    claude_cmd="claude --continue"
  else
    claude_cmd="claude"
  fi

  if has_codex_session "$root"; then
    codex_cmd="codex resume --last --sandbox workspace-write -a never"
  else
    codex_cmd="codex --sandbox workspace-write -a never"
  fi

  # 4) コマンドを自動起動
  if [[ "$NO_UPDATE" == "true" ]]; then
    # アップデートなし（オフライン用）
    tmux send-keys -t "$session:dev.1" "$claude_cmd" C-m
    tmux send-keys -t "$session:dev.2" "$codex_cmd" C-m
    tmux send-keys -t "$session:dev.3" "yazi" C-m
  else
    # アップデートあり（デフォルト）
    tmux send-keys -t "$session:dev.1" "npm update -g @anthropic-ai/claude-code && $claude_cmd" C-m
    tmux send-keys -t "$session:dev.2" "npm update -g @openai/codex && $codex_cmd" C-m
    tmux send-keys -t "$session:dev.3" "yazi" C-m
  fi

  # 5) ペインに @role タグを設定（Agent Teams 動的ペイン管理用）
  tmux set-option -pt "$session:dev.1" @role "claude"
  tmux set-option -pt "$session:dev.2" @role "codex"
  tmux set-option -pt "$session:dev.3" @role "yazi"

  # 6) git window（差分やログ用）
  tmux new-window -t "$session" -n git -c "$root"
  tmux send-keys -t "$session:git" "echo '🌿 git (status/diff/log)'" C-m

  tmux select-window -t "$session:dev"
  tmux select-pane -t "$session:dev.1"
fi

exec tmux attach -t "$session"
